import { Typography } from '@mui/material';

export default function Title() {
    return (
        <Typography variant='h4' component='a' href='/main' sx={{ margin: 'auto', color: 'inherit', textDecoration: 'none' }}>
            TITLE
        </Typography>
    );
}
